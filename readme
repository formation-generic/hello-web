
http://localhost:8080/

Packaging Spring Boot application as an executable jar
	<plugin>
  		<groupId>org.springframework.boot</groupId>
  		<artifactId>spring-boot-maven-plugin</artifactId>
	</plugin>
	
	mvn clean package
	
	java -jar 02_Hello-Web-1.jar
	
	Port par défaut défini dans application.properties: 9594
	
	Pour changer le port d'éxecution à l'arrivée.
	java -jar Hello-Web-1.jar --server.port=9595
